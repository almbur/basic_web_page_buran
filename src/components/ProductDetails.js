import React from 'react';

const ProductDetails = ({ productId }) => {
  return (
    <div>
      <h2>Product {productId} Details</h2>
      {/* Add detailed information about the product here */}
    </div>
  );
};

export default ProductDetails;